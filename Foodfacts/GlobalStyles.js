import { StyleSheet } from "react-native";

export const GlobalStyles = StyleSheet.create({
  heading: {
    fontSize: 18,
    fontWeight: "bold",
  },
  container: {
    padding: 8,
  },
  input: {
    borderStyle: "solid",
    borderColor: "black",
  },
});
