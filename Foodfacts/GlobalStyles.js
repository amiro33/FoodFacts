import { StyleSheet } from "react-native";

export const GlobalStyles = StyleSheet.create({
  heading: {
    fontSize: 18,
    fontWeight: "bold",
  },
  container: {
    padding: 8,
    gap: 4,
  },
  input: {
    borderStyle: "solid",
    borderColor: "black",
    borderWidth: 1,
  },
});
